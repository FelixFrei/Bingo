# Bingo
Simple number generator for a Bingo game.

## Usage
Run:

```bash
python3 BingoNumberGenerator.py
```

Each run draws one new number between 1 and 90 and stores drawn numbers in `numbers.csv`.
